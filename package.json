{
  "name": "food-road",
  "engines": {
    "node": "8.12.0",
    "yarn": "1.12.1"
  },
  "scripts": {
    "build": "cd client && npm install --verbose && npm run build --verbose && cd ..",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "npm run build --verbose && npm run deploy --verbose && echo 'Client built!'"
  }
}

